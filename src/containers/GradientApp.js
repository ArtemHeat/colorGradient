import React from 'react';
import ColorForm from './ColorForm';

export default class GradientApp extends React.Component {
  constructor(props) {
    super(props);
  }
  render() {
    return (
      <ColorForm />
    );
  }
}
